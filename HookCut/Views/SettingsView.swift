import SwiftUI

/// Settings view - accessible from app menu
/// Teammate 3 (UI) will implement this fully
struct SettingsView: View {
    @EnvironmentObject var appState: AppState

    var body: some View {
        Text("Settings Placeholder")
            .frame(width: 500, height: 400)
    }
}
