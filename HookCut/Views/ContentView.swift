import SwiftUI

/// Main content view - the primary app layout
/// Teammate 3 (UI) will implement this fully
struct ContentView: View {
    @EnvironmentObject var appState: AppState

    var body: some View {
        Text("HookCut - Content View Placeholder")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
